# =============================================================================
# Fail2Ban - Filtro para bots que buscan vulnerabilidades
# =============================================================================

[Definition]
failregex = ^<HOST> - .* "(GET|POST|HEAD).*(wp-login|xmlrpc|phpmyadmin|admin|\.env|\.git).*" .* .*$
ignoreregex =
